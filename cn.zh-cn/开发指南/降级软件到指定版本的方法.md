# 降级软件到指定版本的方法<a name="ZH-CN_TOPIC_0092042046"></a>

在安装python-openstackclient 3.2.1时，openstackclient依赖的组件会自动升级到比较高的版本，导致openstack部分命令不可用。

如果遇到此类问题，请重新按推荐版本安装相关的组件。

举例：

**pip install os-client-config==1.21.1**

